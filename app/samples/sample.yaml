service:
  name: Service
types:
  - name: Person
    key: ID
    requiredProperties:
      - name
  - name: Blog
    key:
      - name: ID
        type: long
    requiredProperties: Title
    optionalProperties: # Optional
      - name: author
        type: Person
        contained: true # Optional
root:
  # Collection
  - name: Blogs
    type: Blog[]
  - name: OneBlog
    type: Blog
  - name: People
    type: Person[]